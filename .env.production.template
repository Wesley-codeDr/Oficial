# ============================================
# WELLWAVE MVP - ENVIRONMENT VARIABLES (PRODUCTION)
# ============================================
# Copie este arquivo e preencha os valores para deploy no Vercel
# ============================================

# ---- DATABASE (Supabase - Produção) ----
# Obtenha em: Supabase Dashboard → Settings → Database → Connection string
DATABASE_URL="postgresql://postgres.[project-ref]:[YOUR-PASSWORD]@aws-0-sa-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.[project-ref]:[YOUR-PASSWORD]@aws-0-sa-east-1.pooler.supabase.com:5432/postgres"

# ---- SUPABASE KEYS ----
# Obtenha em: Supabase Dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL="https://[project-ref].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJhbGciOi..."
SUPABASE_SERVICE_ROLE_KEY="eyJhbGciOi..."

# ---- OPENAI ----
# Obtenha em: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-proj-..."

# ---- SENTRY (Monitoramento de Erros) ----
# Obtenha em: https://sentry.io → Project Settings → Client Keys (DSN)
SENTRY_DSN="https://xxx@xxx.ingest.sentry.io/xxx"
SENTRY_AUTH_TOKEN="sntrys_..."

# ---- APP CONFIG ----
NEXT_PUBLIC_APP_URL="https://wellwave.vercel.app"
NODE_ENV="production"

# ============================================
# INSTRUÇÕES PARA VERCEL
# ============================================
# 1. Vá para Vercel Dashboard → Project → Settings → Environment Variables
# 2. Adicione cada variável acima
# 3. Para DATABASE_URL e DIRECT_URL, marque apenas "Production"
# 4. Para variáveis NEXT_PUBLIC_*, marque "All Environments"
# 5. Para chaves sensíveis (SERVICE_ROLE, OPENAI), marque apenas "Production"
# ============================================
