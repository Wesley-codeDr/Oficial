# Run lint-staged which includes linting and formatting
pnpm lint-staged

# Note: lint-staged is configured in package.json to run:
# - ESLint with --fix for .ts/.tsx/.js/.jsx files
# - Prettier --write for all relevant files
